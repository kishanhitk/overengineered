apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.34.0 (HEAD)
  labels:
    io.kompose.service: dicedb
  name: dicedb
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: dicedb
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.34.0 (HEAD)
      labels:
        io.kompose.service: dicedb
    spec:
      containers:
        - image: dicedb/dicedb
          name: dicedb
          ports:
            - containerPort: 7379
              protocol: TCP
      restartPolicy: Always
